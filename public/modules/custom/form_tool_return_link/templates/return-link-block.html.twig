{% set link_attributes = {
  'class': [
    'breadcrumb__link',
  ],
} %}

<nav class="breadcrumb" role="navigation">
  {% if return_link_url %}
    {{ link(return_link_text, return_link_url, link_attributes) }}
  {% endif %}
</nav>
